# Temporary development image placeholder.
# Replace with production-ready Dockerfile once the FastAPI code is migrated.
FROM python:3.11-slim
WORKDIR /app
RUN useradd --create-home app && chown app:app /app
USER app
CMD ["sleep", "3600"]
