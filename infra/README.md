# Infrastructure

Infrastructure-as-code, local orchestration, and deployment manifests live here.

Subdirectories:
- `docker/`: development docker-compose files and shared Docker assets.
- `telemetry/`: OpenTelemetry Collector, Prometheus, Grafana configuration.
- `deploy/`: Fly.io/Render manifests (to be created).
